# === Web ===
fastapi
uvicorn[standard]
gradio
slowapi

# === Config ===
pydantic-settings
python-dotenv

# === Embeddings ===
# NOTA: torch s'instal·la a part en CPU-only per estalviar ~2.5 GB de CUDA:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
sentence-transformers

# === LLM ===
langchain-core
langchain-google-genai

# === Ingest (chunking) ===
langchain-text-splitters

# === Vector DBs ===
pinecone      # backend actiu (free tier serverless)
chromadb      # backend futur (local, quan RAM >= 16 GB)
