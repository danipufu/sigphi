# === Web ===
fastapi
uvicorn[standard]
gradio
slowapi

# === Config ===
pydantic-settings
python-dotenv

# === Embeddings ===
# NOTA: torch s'instal·la a part en CPU-only per estalviar ~2.5 GB de CUDA:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
sentence-transformers

# === LLM ===
langchain-core
langchain-google-genai

# === Ingest (chunking) ===
langchain-text-splitters

# === Vector DBs ===
pinecone        # backend free tier serverless (límit 2 GB)
chromadb        # backend local senzill
qdrant-client   # backend self-hosted al VPS (corpus complet, on_disk + quantització)
